<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnit.system.mapper.UserInteractionMapper">

    <resultMap id="BaseResultMap" type="UserInteraction">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="target_type" property="targetType" jdbcType="VARCHAR"/>
        <result column="target_id" property="targetId" jdbcType="VARCHAR"/>
        <result column="interaction_type" property="interactionType" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="com.hnit.system.domain.UserInteraction">
        INSERT INTO user_interactions
            (id, user_id, target_type, target_id, interaction_type, created_at)
        VALUES
            (#{id}, #{userId}, #{targetType}, #{targetId}, #{interactionType}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM user_interactions
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND interaction_type = #{interactionType}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user_interactions
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND interaction_type = #{interactionType}
    </select>

    <select id="selectByUserAndType" resultMap="BaseResultMap">
        SELECT * FROM user_interactions
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND interaction_type = #{interactionType}
        ORDER BY created_at DESC
    </select>

</mapper>