<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnit.system.mapper.ActivityParticipantsMapper">
    <resultMap id="BaseResultMap" type="com.hnit.system.domain.ActivityParticipants">
        <id column="id" property="id"/>
        <result column="activity_id" property="activityId"/>
        <result column="user_id" property="userId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO activity_participants (id, activity_id, user_id, status, create_time)
        VALUES (#{id}, #{activityId}, #{userId}, #{status}, NOW())
    </insert>

    <update id="updateStatus">
        UPDATE activity_participants SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteByActivityAndUser">
        DELETE FROM activity_participants
        WHERE activity_id = #{activityId} AND user_id = #{userId}
    </delete>

    <select id="selectByActivityAndUser" resultMap="BaseResultMap">
        SELECT * FROM activity_participants
        WHERE activity_id = #{activityId} AND user_id = #{userId}
    </select>

    <select id="selectByActivityId" resultMap="BaseResultMap">
        SELECT * FROM activity_participants WHERE activity_id = #{activityId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM activity_participants WHERE user_id = #{userId} AND status = 1
    </select>
</mapper>