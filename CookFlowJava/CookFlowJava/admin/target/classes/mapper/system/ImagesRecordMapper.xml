<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnit.system.mapper.ImagesRecordMapper">
    <resultMap id="ImagesRecordResult" type="com.hnit.system.domain.ImagesRecord">
        <id property="id" column="id"/>
        <result property="tableName" column="table_name"/>
        <result property="recipeId" column="recipe_id"/>
        <result property="imageId" column="image_id"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <resultMap id="BaseResultMap" type="ImagesRecord">
        <id column="id" property="id"/>
        <result column="table_name" property="tableName"/>
        <result column="recipe_id" property="recipeId"/>
        <result column="image_id" property="imageId"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, table_name, recipe_id, image_id, sort, create_time
    </sql>

    <select id="selectByRecipeId" resultMap="ImagesRecordResult">
        SELECT <include refid="Base_Column_List"/>
        FROM images_record
        WHERE recipe_id = #{recipeId}
        ORDER BY sort ASC, create_time ASC
    </select>

    <insert id="insert" parameterType="ImagesRecord">
        INSERT INTO images_record (id, table_name, recipe_id, image_id, sort)
        VALUES (#{id}, #{tableName}, #{recipeId}, #{imageId}, #{sort})
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO images_record (id, table_name, recipe_id, image_id, sort, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.tableName}, #{item.recipeId}, #{item.imageId}, #{item.sort}, #{item.createTime})
        </foreach>
    </insert>

    <update id="update" parameterType="ImagesRecord">
        UPDATE images_record
        <set>
            <if test="tableName != null">table_name = #{tableName},</if>
            <if test="recipeId != null">recipe_id = #{recipeId},</if>
            <if test="imageId != null">image_id = #{imageId},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM images_record WHERE id = #{id}
    </delete>

    <delete id="deleteByRecipeId">
        DELETE FROM images_record WHERE recipe_id = #{recipeId}
    </delete>

    <select id="selectByTableAndRecordId" resultType="ImagesRecord">
        SELECT * FROM images_record
        WHERE table_name = #{tableName} AND recipe_id = #{recordId}
        ORDER BY sort ASC, create_time ASC
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM images_record
        WHERE table_name = #{tableName}
          AND recipe_id = #{recipeId}
        ORDER BY sort ASC
    </select>

    <select id="selectListByRecipeIds" resultMap="BaseResultMap">
        SELECT * FROM images_record
        WHERE table_name = #{tableName}
        AND recipe_id IN
        <foreach collection="recipeIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY sort ASC
    </select>
</mapper>